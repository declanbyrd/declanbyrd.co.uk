---
title: My Photos
description: Photos I've taken!
flickrUrl: https://www.flickr.com/photos/declanbyrd
eleventyNavigation:
  key: Photos
  order: 3
---

{% extends 'layouts/page.njk' %} 

{% block content %}
  <h2 class="page-title">{{ title }}</h2>
  <p>
    This is where you will find photos that I post to my
    <a href="{{ flickrUrl }}">Flickr</a> account. There's no likes, followers or
    24 hour vanishing stories here.
  </p>
  <section class="photo-gallery">
    {% for photo in photos %}
      <a href="{{ flickrUrl }}/{{ photo.id }}">
        <img
          loading="lazy"
          class="photo-item"
          src="{{ photo.url }}"
          alt="Photo of {{ photo.caption }}"
        />
        <span class="hidden">Link to Flickr page for {{photo.caption}}</span>
      </a>
    {% endfor %}
  </section>
{% endblock %}
