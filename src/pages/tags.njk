---
pagination:
  data: collections
  size: 1
  alias: tag
permalink: /journal/tags/{{ tag | slug }}/
eleventyComputed:
  title: Journal entries about {{ tag }}. 
  description: All of the journal posts on Declan Byrd's website about {{ tag }}.
layout: base
---
{% set postslist = collections[tag] | reverse %}

{% block content %}
  <section>
    <h1 >Results for: <span class="accent">{{ tag }}</span></h1>
    {% if postslist.length > 1 %}
      <p>There are {{ postslist.length }} journal entries for {{ tag }}:</p>
    {% endif %}
    {% if postslist.length == 1 %}
      <p>There is {{ postslist.length }} journal entry for {{ tag }}:</p>
    {% endif %}
    {%include "partials/postsList.njk" %}
    <p>
    See <a href="{{ '/journal/tags/' | url }}">more tags</a> that I have written about.
  </p>
  </section>
{% endblock %}