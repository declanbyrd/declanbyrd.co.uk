<ul class="journal">
  {% for post in postslist %}
    <li>
      <h3>
        <a href="{{ post.url | url }}">
          {{ post.data.title }}
        </a>
      </h3>
      <time datetime="{{ post.date | htmlDateTime }}" class="[ date ]">
        {{ post.date | readableDates }}
      </time>

      <p>{{post.data.description}}</p>
    </li>
  {% endfor %}
</ul>