<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <link rel="prefetch" as="font" type="font/truetype" crossorigin href="/assets/fonts/Comfortaa.ttf">
    <link rel="prefetch" as="font" type="font/truetype" crossorigin href="/assets/fonts/Montserrat.ttf">
    <link rel="icon" type="image/svg+xml" href="/assets/icons/favicon.svg">
    <link rel="alternate icon" href="/assets/icons/favicon.jpg">
    <title>{{renderData.title or title or metadata.title}}</title>
    <meta name="Description" content="{{renderData.description or description or metadata.description}}"/>
    <style>
      {% include "css/styles.css" %}
    </style>
  </head>
  <body>
    <a class="[ skip-link ]" href="#mainContent">Skip to content</a>
    <header class="[ page-header ]">
      <div class="[ marquee-section ]">
        <h1>
          <a href="{{ '/' | url }}" class="[ hover-link ]{% if entry.url == '/' %} [ active ]{% endif %}">{{ metadata.title }}</a>
        </h1>
        {% include '_navigation.njk' %}
      </div>
    </header>

    <main id="mainContent" class="[ content-body ]">
      {% block content %}
        {{ content | safe }}
      {% endblock %}
    </main>

    <footer class="[ marquee-section ]">
      {% include '_footer-navigation.njk' %}
      {% include '_social-links.njk' %}
      <script type="text/javascript" src="/assets/scripts/main.js" defer></script>
    </footer>
  </body>
</html>