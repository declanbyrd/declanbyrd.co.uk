<nav>
  <ul class="[ horizontal-links ]">
    {%- for entry in collections.all | eleventyNavigation %}
    <li>
      <a href="{{ entry.url | url }}"
        class="[ hover-link ] {% if entry.url == page.url %} [ active ]{% endif %}">{{ entry.title }}</a>
    </li>
    {%- endfor %}
  </ul>
</nav>