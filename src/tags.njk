---
pagination:
  data: collections
  size: 1
  alias: tag
permalink: /tags/{{ tag | slug }}/
renderData:
  title: Journal entries about {{ tag }}. 
  description: All of the journal posts on Declan Byrd's website about {{ tag }}.
---

{% extends 'layouts/base.njk' %}
{% set postslist = collections[tag].reverse() %}

{% block content %}
  <section>
    <h2 >Results for: <span class="accent">{{ tag }}</span></h2>
    {% if postslist.length > 1 %}
      <p>There are {{ postslist.length }} journal entries about {{ tag }}:</p>
    {% endif %}
    {% if postslist.length == 1 %}
      <p>There is {{ postslist.length }} journal entry about {{ tag }}:</p>
    {% endif %}
    {%include "partials/postsList.njk" %}
    <p>
    See <a href="{{ '/tags/' | url }}">more tags</a> that I have written about.
  </p>
  </section>
{% endblock %}